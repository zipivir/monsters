<h1>Listing foods</h1>

<table>
  <tr>
    <th>Monster food</th>
    <th>Name</th>
    <th>Taste</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @foods.each do |food| %>
  <tr>
    <td><%= food.monster_food %></td>
    <td><%= food.name %></td>
    <td><%= food.taste %></td>
    <td><%= link_to 'Show', food %></td>
    <td><%= link_to 'Edit', edit_food_path(food) %></td>
    <td><%= link_to 'Destroy', food, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Food', new_food_path %>
